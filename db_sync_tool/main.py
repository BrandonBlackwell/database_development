from cli.parser import run_cli

if __name__ == "__main__":
    run_cli()
# Entry point for the database synchronization tool.
# This script initializes and runs the command-line interface for the tool.
# It imports the run_cli function from the cli.parser module and executes it when the script is run directly.
# No additional code needed here.
